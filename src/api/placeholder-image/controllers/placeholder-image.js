'use strict';

/**
 * placeholder-image controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::placeholder-image.placeholder-image');
